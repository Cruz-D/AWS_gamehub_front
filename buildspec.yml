version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 8.0

  build:
    commands:
      - echo "Publicando aplicación..."
      - dotnet publish AWS_gamehub_front.csproj -c Release -r linux-x64 --self-contained false -o publish

artifacts:
  base-directory: publish
  files:
    - '**/*'